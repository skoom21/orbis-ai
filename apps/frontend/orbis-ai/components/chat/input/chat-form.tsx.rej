--- chat-form.tsx
+++ chat-form.tsx
@@ -625,6 +625,31 @@
             />
           </div>
         </div>
+        
+        {isListening && (
+          <div className="absolute inset-0 z-10 flex items-center justify-center bg-background/80 backdrop-blur-sm rounded-3xl border border-primary/20">
+            <div className="flex flex-col items-center gap-3">
+              <div className="flex items-center gap-1.5 h-8">
+                {[...Array(5)].map((_, i) => (
+                  <div 
+                    key={i} 
+                    className="w-1.5 bg-primary/80 rounded-full animate-pulse"
+                    style={{ 
+                      height: \`\${Math.max(20, Math.random() * 100)}%\`,
+                      animationDelay: \`\${i * 0.15}s\`,
+                      animationDuration: '0.8s'
+                    }}
+                  />
+                ))}
+              </div>
+              <p className="text-sm font-medium text-primary animate-pulse">Listening...</p>
+            </div>
+            <Button variant="ghost" size="icon" className="absolute right-4 top-1/2 -translate-y-1/2" onClick={toggleListening}>
+              <X className="h-5 w-5 text-muted-foreground" strokeWidth={1.5} />
+              <span className="sr-only">Cancel listening</span>
+            </Button>
+          </div>
+        )}
 
         {showControls && (
           <div id={controlsId} className="flex justify-between items-center mt-2 px-1 pb-1">
